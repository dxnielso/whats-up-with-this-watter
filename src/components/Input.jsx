const Input = ({placeholder, type}) => {
  return (
    <input type={type} name="" id="" placeholder={placeholder} className='w-full px-3 py-2 border mb-5 text-base rounded-md outline-none text-gray-900 font-normal' />
  )
}

export default Input